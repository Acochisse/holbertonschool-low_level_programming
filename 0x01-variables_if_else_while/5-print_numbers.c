#include <stdio.h>
/**
 * main - Entry point
 * This program prints all single d # b10
 * Return: 0 (success)
 */
int main(void)
{
	int x;

	for (x = 0; x < 10; x++)
		printf("%i", x);
	putchar('\n');
	return (0);
}
