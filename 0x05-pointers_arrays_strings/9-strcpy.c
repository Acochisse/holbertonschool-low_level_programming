#include "holberton.h"

/**
 * _strcpy - copies string to a destination
 * @dest: destination
 * @src: input
 *
 * Return: point to destination
 */
char *_strcpy(char *dest, char *src)
{
	int i;

	for (i = 0; (dest[i] = src[i]) != '\0'; i++)
		:
	return (dest);
}
